__pycache__/
*.pyc
*.egg-info/
dist/
build/
.venv/
.pytest_cache/

# Internal-only docs (kept locally, not for public repo)
CLAUDE.md

# Model artifacts (large, regenerated by training)
models/*.pt
models/*.onnx
models/training.log
!models/.gitkeep

# Web build artifacts
web/node_modules/
web/public/models/*.onnx
# web/dist/ — generated by `npm run build`, never committed (ignored via top-level dist/ above)
